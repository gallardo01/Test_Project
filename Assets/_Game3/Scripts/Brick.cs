using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Brick : ColorObject
{
    // [SerializeField] private List<Material> listMaterials = new List<Material>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // public void SetMaterialByID(int indexID){
    //     gameObject.GetComponent<Renderer>().material = listMaterials[indexID];
    // }

}
