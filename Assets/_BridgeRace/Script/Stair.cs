using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stair : ColorObject
{
    // Start is called before the first frame update
    void Start()
    {
        ChangeColor(ColorType.Default);
    }
}
